---

- name: setup PIA-Tunnel with Debian
  hosts: pia-tunnel-debian
  remote_user: root
  vars:
    git_branch: "release_php-gui-v2"

  roles:
    - common
    - apache
    - php
    - openvpn
    - pia