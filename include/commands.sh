#!/bin/bash
# holds paths to commands as variables
# collect commands for now, then split into commands-debian.sh and commands-freebsd.sh, then use this file to source one of the two

# BSD CMD_GREP='/usr/bin/grep'
CMD_GREP='/bin/grep'

# BSD CMD_GIT='/usr/local/bin/git'
CMD_GIT='/usr/bin/git'

# BSD CMD_GAWK='/usr/local/bin/gawk'
CMD_GAWK='/usr/bin/gawk'

# BSD CMD_PING='/sbin/ping'
CMD_PING='/sbin/ping'

# BSD CMD_IP='/sbin/ip'
CMD_IP='/bin/ip'

# BSD CMD_CUT='/usr/bin/cut'
CMD_CUT='/usr/bin/cut'

CMD_SED='/bin/sed'