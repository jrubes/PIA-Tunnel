# dhcpd.conf sample file used to reconfigure the DHCP Service
# option definitions common to all supported networks...
option domain-name "localdomain.local";
default-lease-time 600;
max-lease-time 7200;

# Use this to enble / disable dynamic dns updates globally.
#ddns-update-style none;

# If this DHCP server is the official DHCP server for the local
# network, the authoritative directive should be uncommented.
authoritative;

# Use this to send dhcp log messages to a different log file (you also
# have to hack syslog.conf to complete the redirection).
log-facility local7;

option domain-name-servers DNSSERVER_HERE;

# A slightly different configuration for an internal subnet.
subnet SUBNET_IP_HERE netmask NETWORK_MASK_HERE {
  range IP_RANGE_HERE;
  option routers ROUTER_IP_HERE;
  option broadcast-address BROADCAST_HERE;
}

# example of static range - not supported by PHP GUI ATM
#host statichost {
#  hardware ethernet 08:00:07:26:c0:a5;
#  fixed-address 192.168.10.101;
#}
